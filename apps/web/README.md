# apps/web

Next.js 16 application (App Router). Route pages under `src/app/[locale]/`. Consumes libs (content, i18n, seo, screens, sections, ui, tokens, a11y) and runs the verify-chain validators via Nx targets.
