import { CookiePolicyScreen, cookiesMetadata } from '@joelklemmer/screens';

export const generateMetadata = cookiesMetadata;

export default function CookiesPage() {
  return <CookiePolicyScreen />;
}
