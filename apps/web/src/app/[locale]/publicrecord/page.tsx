import { PublicRecordScreen, publicRecordMetadata } from '@joelklemmer/screens';

export const generateMetadata = publicRecordMetadata;

export default function PublicRecordPage() {
  return <PublicRecordScreen />;
}
