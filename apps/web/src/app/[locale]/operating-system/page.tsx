import {
  OperatingSystemScreen,
  operatingSystemMetadata,
} from '@joelklemmer/screens';

export const generateMetadata = operatingSystemMetadata;

export default function OperatingSystemPage() {
  return <OperatingSystemScreen />;
}
