import { PreferencesScreen, preferencesMetadata } from '@joelklemmer/screens';

export const generateMetadata = preferencesMetadata;

export default function PreferencesPage() {
  return <PreferencesScreen />;
}
