# Site URL used for canonical links and JSON-LD. Set in CI and production.
# NEXT_PUBLIC_SITE_URL=https://joelklemmer.com

# Comma-separated list of verified profile URLs for Person sameAs (e.g. LinkedIn).
# Production should set at least one; omit or leave empty to exclude sameAs from JSON-LD.
# Example: NEXT_PUBLIC_IDENTITY_SAME_AS=https://www.linkedin.com/in/joelklemmer
# NEXT_PUBLIC_IDENTITY_SAME_AS=

# Set to 1 in release builds to enforce required artifacts, proof files, and sameAs.
# RELEASE_READY=0
